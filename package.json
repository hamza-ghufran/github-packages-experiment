{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test --since",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}